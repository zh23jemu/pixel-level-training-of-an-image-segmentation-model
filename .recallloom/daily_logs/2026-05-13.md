<!-- recallloom:file=daily_log version=1.0 lang=zh-CN -->
<!-- daily-log-entry: entry-id=entry-1 | created-at=2026-05-13T13:49:09+08:00 | writer-id=Codex | entry-seq=1 -->

<!-- section: work_completed -->
# 完成工作

- 初始化并接管了本项目的 RecallLoom sidecar。
- 已写入 `context_brief.md` 和 `rolling_summary.md`，并通过 `validate_context.py` 验证。
- 当前项目上下文已更新为：等待真实 `iccv09Data.tar.gz` 数据集进入正式训练阶段。

<!-- section: confirmed_facts -->
# 确认事实

- 项目代码和配置已经完成首版实现，并通过本地单元测试与 smoke test。
- 数据集只需要 `iccv09Data.tar.gz` 这一个压缩包即可，不需要额外单独下载标注。
- 训练与评估流程统一使用项目本地 `.venv`。

<!-- section: key_decisions -->
# 关键决策

- RecallLoom 使用隐藏 sidecar 模式，工作区语言设为 `zh-CN`。
- 后续正式实验按 `ce`、`dice`、`ce_dice` 三组配置顺序执行。

<!-- section: risks_blockers -->
# 风险与阻塞

- 真实数据尚未放入 `data/`，因此正式训练结果和最终 PDF 还未生成。
- 当前阶段需要等待数据到位后继续训练与评估。

<!-- section: recommended_next_step -->
# 建议下一步

- 将 `iccv09Data.tar.gz` 放入 `data/` 并运行 `scripts/prepare_data.py --root data --skip-download`。
- 随后依次训练三组配置，补齐权重、测试结果和实验报告。

<!-- daily-log-entry: entry-id=entry-2 | created-at=2026-05-13T13:58:43+08:00 | writer-id=Codex | entry-seq=2 -->

<!-- section: work_completed -->
# 完成工作

- 用户确认数据集已解压。
- 更新 `.gitignore`，将 `data/raw*/` 和 `data/iccv09Data/` 纳入忽略范围，同时继续忽略 `data/*.tar.gz`。
- 更新 RecallLoom 当前状态，记录数据已就绪且下一步进入真实数据检查与正式训练。

<!-- section: confirmed_facts -->
# 确认事实

- 当前 `data/` 下存在 `iccv09Data.tar.gz`、`raw/` 和 `raw_20260513_135335/`。
- 时间戳解压目录包含 `iccv09Data/images` 与 `iccv09Data/labels`。

<!-- section: key_decisions -->
# 关键决策

- 数据集压缩包和解压目录默认视为本地外部数据，不进入 Git 提交范围。
- 不删除多余数据目录；如需要清理旧目录，应由用户确认后处理。

<!-- section: risks_blockers -->
# 风险与阻塞

- 当前有两个解压目录，训练发现逻辑应先做一次真实数据检查以确认实际使用目录。
- 正式训练、最终权重和 PDF 报告仍未生成。

<!-- section: recommended_next_step -->
# 建议下一步

- 用 `.venv` 运行一个真实数据读取检查，确认样本数量、图像/标签配对和 mask 尺寸。
- 检查通过后启动三组训练配置。
